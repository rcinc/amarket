<p id="notice"><%= notice %></p>

<br>
<br>
<h1>Art Gallery</h1>

<table>
  <thead>
    <tr>
      <th></th>
      <th>Name</th>

      <th>Price</th>

      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @products.each do |product| %>
      <tr>
        <td><%= image_tag(product.url) %></td><br>
        <td><%= product.name %></td>

        <td><%= product.price %></td>

        <td><%= link_to 'Show More', product %></td>
        <% if current_user.try(:id) == product.user_id %>
        <td><%= link_to 'Edit', edit_product_path(product) %></td>
        <td><%= link_to 'Destroy', product, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        <% end %>
      </tr>
    <% end %>
  </tbody>
</table>



<br>
<% if current_user.try(:artist?) %>
<%= link_to 'New Product', new_product_path %>
<% end %>
